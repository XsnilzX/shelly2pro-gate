{
  "name": "homebridge-shelly2pro-gate",
  "version": "1.0.0",
  "description": "Homebridge plugin to control a gate via Shelly 2 Pro",
  "main": "dist/platform.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prepublishOnly": "npm run build"
  },
  "keywords": [
    "homebridge-plugin"
  ],
  "engines": {
    "node": ">=16.0.0",
    "homebridge": ">=1.6.0"
  },
  "dependencies": {
    "axios": "^1.6.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "homebridge": "^1.6.0",
    "@types/node": "^18.0.0"
  }
}
